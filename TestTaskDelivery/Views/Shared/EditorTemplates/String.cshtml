@model string

<div class="d-flex align-items-center mb-3 flex-column flex-md-row">
    <label asp-for="@Model" class="flex-shrink-0 mb-2 mb-md-0 fw-semibold" style="width: 180px;"></label>
    <textarea asp-for="@Model" class="form-control flex-grow-1 field-max-width" placeholder="Введите адрес..."></textarea>
</div>